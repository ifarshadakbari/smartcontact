# دستورالعمل اختصاصی توسعه پروژه (AGENTS.md)

1. **حفظ ساختار مونوریپو و عدم دستکاری روت و بک‌اند**:
   هوش مصنوعی برای اعمال تغییرات در فرانت‌اند پروژه به هیچ عنوان نباید به پوشه `backend/` دست بزند، ساختار مونوریپو را تغییر دهد یا فایل‌های ریشه (Root) را جابه‌جا و بازآرایی کند. تمامی تغییرات کلاینت و رابط کاربری منحصراً در پوشه `frontend/` اعمال می‌شوند. فایل‌ها و منطق مربوط به سرویس‌دهنده و لاراول در ساب‌فولدر `backend/` قرار دارند. در صورت نیاز به تغییرات دیتابیس یا مایگریشن در بک‌اند، راهنمای دقیق دستورات و فایل‌ها ارائه شود تا روی سرور اعمال گردد.

2. **عدم سینک مستقیم تغییرات روت به سرور**:
   در هنگام git pull روی سرور، تغییرات مربوط به فایل‌های روت AI Studio باید نادیده گرفته شوند یا صرفاً تغییرات پوشه `frontend/` مرج گردند.

3. **معماری مونوریپو و سرور توسعه فرانت‌اند**:
   این پروژه یک مونوریپو است؛ سرور اجرایی فرانت‌اند در پوشه `frontend/` مستقر است و نیازی به اجرای dev server در پوشه ریشه نیست. خطاهای عدم اجرای dev server در روت نادیده گرفته شوند.
